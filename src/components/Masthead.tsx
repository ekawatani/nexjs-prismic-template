import React from 'react';
import { Heading } from './Heading';

export const Masthead: React.FunctionComponent = () => {
  return (
    <div>
      <Heading level={1}>Masthead</Heading>
    </div>
  );
};
