{
  "name": "fonts",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "preinstall": "./scripts/preinstall.sh",
    "postinstall": "pipenv install",
    "build": "pipenv run python3 build-fonts.py"
  }
}
